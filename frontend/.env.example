# Optional overrides for the Vite dev-server proxy targets.
# Defaults (used when unset): backend at http://localhost:8000 and ws://localhost:8000.
VITE_BACKEND_ORIGIN=http://localhost:8000
VITE_BACKEND_WS_ORIGIN=ws://localhost:8000

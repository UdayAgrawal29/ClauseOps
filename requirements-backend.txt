# ClauseOps Web Platform - Backend dependencies
# Install: pip install -r requirements-backend.txt
#
# These are the dependencies for the FastAPI + async SQLAlchemy backend in the
# `app/` package. They are kept separate from the root requirements.txt (which
# covers the existing `clauseops` ML package). The ML package is imported and
# run by the heavy Celery `ml` worker and is never rewritten.

# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.7

# --- ORM + migrations (async SQLAlchemy 2.0) ---
SQLAlchemy[asyncio]>=2.0.0
asyncpg>=0.29.0            # async PostgreSQL driver
alembic>=1.13.0

# --- Validation / settings ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- Task queue / broker / pub-sub ---
celery>=5.3.0
redis>=5.0.0

# --- Auth (own JWT, email + password) ---
passlib[argon2]>=1.7.4
argon2-cffi>=23.1.0
pyjwt>=2.8.0              # JWT issue/verify/refresh

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
hypothesis>=6.0.0
